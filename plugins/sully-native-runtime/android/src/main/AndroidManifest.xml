<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- Android 14+ (API 34) requires a specific foreground-service-type permission.
         This companion runtime is not real telephony/data-sync, so we use SPECIAL_USE
         (no 6h/day cap) for always-on/call/generation and MEDIA_PLAYBACK for music. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <!-- Durable "to-the-minute" wake for scheduled role events / VR / world timers. -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />

    <application>
        <receiver
            android:name="com.sullyos.nativeruntime.SullyNativeRuntimeBootReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
        <receiver
            android:name="com.sullyos.nativeruntime.SullyNativeRuntimeAlarmReceiver"
            android:enabled="true"
            android:exported="false" />
        <service
            android:name="com.sullyos.nativeruntime.SullyNativeRuntimeService"
            android:exported="false"
            android:foregroundServiceType="specialUse|mediaPlayback">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Persistent AI companion always-on runtime for scheduled role events, calls and music playback." />
        </service>
    </application>
</manifest>
