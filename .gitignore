# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
.npm-cache
dist
dist-ssr
*.local

# build artifact — regenerated by `npm run build:instant-worker`;
# committed copy lives at worker/instant-push/worker.bundle.js
public/instant-worker.bundle.js

# build artifact — `pnpm run build:workers` 原样复制过来的，与源文件逐字节相同；
# 提交的那份在 worker/amsg/deno-proxy.ts
public/amsg-deno-proxy.ts

# build artifact — 每次 `pnpm run build` 都会重新打，跟着提交只会让跑完构建的人
# 平白多一条 diff。提交的那份在 worker/amsg/worker.bundle.js（wrangler.toml 的
# main 指着它，手动 wrangler deploy 要用）。
#
# 发布链路都会自己生成，不会因为仓库里没有它就少东西：
#   - GitHub Pages：deploy-pages.yml 跑 `pnpm run build`
#   - Netlify：本地构建后上传 dist
#   - 用户自部署的 worker 仓库：sync-workers-repo.yml 跑 `pnpm build:workers`
public/amsg-worker.bundle.js

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.qoder/
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Claude Code 本地配置/缓存（不入仓库）
.claude/

# xiaohongshu-skills (cloned dependency)
xiaohongshu-skills/

# Local XHS Bridge access token (generated by scripts/start-xhs.bat)
.xhs-bridge-token
scripts/.xhs-bridge-token

# git worktrees
.worktrees/

# WorkBuddy agent 本地工作区（记忆/状态，绝不提交）
.workbuddy/

# xiaohongshu-skills 本地克隆目录（用户重命名版，与已忽略的 xiaohongshu-skills/ 依赖不同）
xiaohongshu-skills-main/

# 本地审计/盘点稿（不入仓库）
提示词注入审计清单.md

# AGENTS.md：本次是针对 merge 修复的本地临时工作指引，不入仓库
AGENTS.md

# wrangler 本地开发：.dev.vars 装的是 worker secrets（master key / VAPID 私钥），
# .wrangler/ 是本地 D1 数据与缓存，两样都不进仓库。
**/.dev.vars
**/.dev.vars.*
!**/.dev.vars.example
**/.wrangler/

.pnpm-store/

# TIAO-Capacitor local Android toolchain and caches (D: only, never committed)
.android-sdk/
.gradle-user-home/
